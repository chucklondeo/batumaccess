SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_USER=sales@batumaccess.com
SMTP_PASS=your_hostinger_email_password

# Supabase CMS backend. Public/publishable values only — RLS policies
# restrict writes, so these are safe to expose to the browser and to bake
# into the static build.
NEXT_PUBLIC_SUPABASE_URL=https://gsfwnzuvnzmfczyzefrd.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_or_publishable_key
